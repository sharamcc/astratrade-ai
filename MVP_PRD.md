# AstraTrade AI MVP 产品需求文档

**版本**：0.1
**状态**：方案评审稿
**日期**：2026-09-20
**本阶段**：只做产品定义，不编码、不接入真实账户、不进行真实交易

## 1. 产品定位

AstraTrade AI 是一个面向多用户的 OKX AI 交易 Agent。用户授权自己的 OKX 账户后，选择已审核的策略和风险档位；Agent 在确定性的风控规则内生成并执行交易，用户可以随时查看、暂停和撤销授权。

产品不托管用户资金，不提供提币能力，不承诺收益，也不允许 AI 绕过风控直接下单。

## 2. 当前原型与 MVP 的差距

当前仓库是单账户 Python 策略监控/执行器，已经具备行情读取、策略触发判断、账户查询和基础下单能力。它仍存在以下限制：

- 依赖本地交易计划和本地凭证；
- 没有用户、租户、授权和权限体系；
- 没有多用户数据隔离；
- 没有完整的订单状态对账和审计模型；
- 策略与执行逻辑尚未服务化；
- 尚未完成 OAuth、Builder Code 归因和多用户 Web 产品。

因此，MVP 的目标是产品化和安全化，而不是简单增加一个网页界面。

## 3. MVP 目标与非目标

### 3.1 目标

1. 用户可以安全连接自己的 OKX 账户。
2. 用户可以在模拟盘中运行一个已审核策略。
3. 用户可以设置风险档位并随时暂停 Agent。
4. 系统可以完整记录信号、风控决定、订单状态和成交结果。
5. 小范围受邀用户可以在明确授权下进行受限实盘测试。
6. 合规订单可以通过 Builder Code 归因。

### 3.2 非目标

- 多交易所支持；
- 用户上传任意交易代码；
- 跟单、社交交易或资金托管；
- 提币、资金划转或借贷；
- 面向公众的开放注册；
- 多策略组合优化和自动调参；
- 收益保证、预测收益或投资建议。

## 4. 目标用户与角色

### 4.1 目标用户

首版面向熟悉数字资产交易、愿意授权 OKX 账户并接受自动化交易风险的受邀测试用户。

### 4.2 角色

| 角色 | 权限 |
|---|---|
| 用户 | 连接/撤销账户、选择策略、设置风险、启动/暂停 Agent、查看交易记录 |
| 运营管理员 | 查看系统健康、管理受邀用户、执行全局暂停 |
| 策略管理员 | 发布、停用、回滚已审核的策略版本 |
| 风控管理员 | 配置平台级限额和熔断规则 |
| 执行服务 | 只处理已授权、已通过风控的订单意图 |

任何管理员都不能读取用户交易凭证明文，也不能绕过审计和风控强制下单。

## 5. 核心用户流程

```text
注册/登录
  → 阅读风险披露并确认
  → OAuth 授权连接 OKX
  → 选择策略与风险档位
  → 默认模拟盘运行
  → 查看信号、风控判断与订单结果
  → 用户主动开启实盘
  → Agent 在限额内执行
  → 用户暂停、撤销授权或退出
```

## 6. MVP 功能需求

### P0：必须具备

| 编号 | 功能 | 验收标准 |
|---|---|---|
| P0-01 | 用户登录 | 用户可登录、退出，账户状态可审计 |
| P0-02 | OKX OAuth 连接 | 用户可授权、查看状态、撤销授权；前端不接触密钥 |
| P0-03 | 模拟盘模式 | 默认不产生真实订单，能展示信号和模拟结果 |
| P0-04 | 策略选择 | 只能选择已发布策略和支持的标的 |
| P0-05 | 风险档位 | 设置单笔风险、最大仓位、杠杆、日亏损和交易次数上限 |
| P0-06 | Agent 启停 | 用户可启动、暂停；暂停后不得产生新订单 |
| P0-07 | 订单生命周期 | 能区分意图、提交、接受、部分成交、成交、撤销、失败和对账完成 |
| P0-08 | 风控拒绝原因 | 每次拒绝都显示规则和可理解的原因 |
| P0-09 | 审计记录 | 能追溯用户、策略版本、行情快照、风控结果和订单结果 |
| P0-10 | 紧急暂停 | 用户和管理员都能触发；管理员可执行全局暂停 |
| P0-11 | Builder 归因 | 合规下单请求写入正确 Builder Code，并可核对结果 |
| P0-12 | 告警 | 授权失效、订单异常、对账失败、熔断和系统错误可通知 |

### P1：首轮灰度后再做

- 多个策略实例；
- WebSocket 行情；
- 更细粒度的策略参数；
- 移动端适配；
- 报表和 CSV 导出；
- 更丰富的通知渠道。

## 7. 风控规则

订单只有同时满足以下条件才可提交：

- 用户授权有效；
- Agent 处于运行状态；
- 策略版本已发布且未过期；
- 标的和订单类型在白名单内；
- 信号仍在有效期内且没有重复执行；
- 单笔风险、仓位、杠杆、日亏损和交易次数均未超限；
- 行情、账户和持仓数据未过期；
- 不存在未解决的异常订单；
- 系统和平台未触发熔断。

以下情况默认拒绝新增风险：行情延迟、账户状态未知、授权状态未知、订单状态未知、归因字段无法写入、风控服务不可用或系统时间异常。

## 8. 关键状态机

### 8.1 Agent 状态

```text
Draft → Simulating → ReadyForLive → Running
Running → Paused → Running
Running → Revoked / Suspended / Failed
```

### 8.2 订单状态

```text
Intent
  → RiskApproved
  → Submitted
  → Accepted / Rejected
  → PartiallyFilled / Filled / Cancelled / Failed
  → Reconciled
```

## 9. MVP 成功指标

技术和运营指标优先于交易收益：

- 100% 订单可关联到用户、策略版本和 Builder Code；
- 重试不产生重复订单；
- 模拟盘订单状态对账成功率达到 99% 以上；
- 风控拒绝都有明确原因；
- 紧急暂停后不再产生新订单；
- 测试期间无凭证泄露；
- 所有实盘订单均能在控制台和本地审计记录中核对。

收益、胜率和返佣不能作为 MVP 上线承诺。

## 10. 上线闸门

只有同时满足以下条件，才允许受邀用户实盘：

1. 产品、技术、安全和合规评审通过。
2. 模拟盘全链路测试通过。
3. OAuth 授权、撤销和过期流程通过。
4. 订单幂等、对账、暂停和熔断演练通过。
5. Builder Code 归因验证通过。
6. 风险披露、用户条款和隐私说明完成审阅。
7. 已定义异常升级、人工介入和回滚流程。

