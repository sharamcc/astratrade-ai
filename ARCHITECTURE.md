# AstraTrade AI 架构设计

**版本**：0.1
**状态**：方案评审稿
**日期**：2026-09-20
**本阶段**：只做架构设计，不编码、不接入真实账户

## 1. 架构目标

架构必须优先保证：

- 多用户数据和凭证隔离；
- 任何订单都经过确定性风控；
- 订单提交可幂等、可对账、可审计；
- AI/策略不能绕过执行边界；
- 用户可以暂停、撤销授权；
- 故障时默认停止新增风险。

首版采用模块化单体，保留后续拆分执行服务、风控服务和通知服务的边界。早期重点是数据一致性和可验证性，而不是服务数量。

## 2. 逻辑架构

```text
┌──────────────────────────────────────┐
│ Web 控制台                            │
│ 登录 / OAuth / 策略 / 风险 / 订单      │
└────────────────┬─────────────────────┘
                 │ HTTPS
┌────────────────▼─────────────────────┐
│ 应用 API                             │
│ 用户、策略、配置、权限、查询、管理后台  │
└──────┬─────────┬──────────┬──────────┘
       │         │          │
┌──────▼───┐ ┌───▼────┐ ┌───▼────────┐
│授权/凭证  │ │策略服务 │ │风控决策服务 │
│OAuth/Fast│ │信号生成 │ │限额/熔断/审批│
└──────┬───┘ └───┬────┘ └───┬────────┘
       │         │          │
       └─────────▼──────────┘
             交易意图
                 │
        ┌────────▼────────┐
        │ 订单执行与对账服务 │
        └────────┬────────┘
                 │
          OKX OAuth/OpenAPI

PostgreSQL：业务与审计数据
Redis/任务队列：调度、锁、短期状态
密钥服务：凭证加密、轮换与访问控制
监控告警：指标、日志、异常与熔断
```

## 3. 模块职责与边界

| 模块 | 负责 | 明确禁止 |
|---|---|---|
| Web 控制台 | 用户交互、状态展示、确认操作 | 保存或直接处理交易密钥 |
| 应用 API | 鉴权、租户隔离、业务编排 | 直接绕过风控下单 |
| 授权服务 | OAuth 授权、刷新、撤销、状态同步 | 将 Client Secret 暴露给前端 |
| 凭证服务 | 加密存储、按用户取用、轮换 | 日志记录明文凭证 |
| 策略服务 | 行情读取、策略计算、生成交易意图 | 直接调用交易下单接口 |
| 风控服务 | 规则校验、限额、熔断、审批 | 根据自然语言自由放行 |
| 执行服务 | 幂等提交、状态查询、订单对账 | 接受未经风控批准的请求 |
| 审计服务 | 记录决策和状态变化 | 保存完整密钥或授权令牌 |
| 通知服务 | 告警、用户状态通知 | 自动解除高风险暂停 |

关键原则：

```text
AI / 策略输出交易意图
        ↓
确定性风控输出允许/拒绝/需确认
        ↓
订单执行器提交唯一合法订单
```

## 4. 推荐技术决策

### 4.1 应用形态

- 前端：Web 控制台，提供用户端和管理端视图。
- 后端：优先延续 Python 生态，复用当前策略原型，采用模块化单体。
- 数据库：PostgreSQL，作为用户、策略、订单、审计的权威数据源。
- 调度：任务队列配合分布式锁，避免同一用户/策略重复运行。
- 缓存：Redis 只保存短期状态、锁和队列信息，不作为订单最终事实来源。
- 密钥：使用云密钥服务或等价的密钥管理系统，数据库只保存加密材料和引用。

### 4.2 多租户隔离

每一条用户相关数据都必须带 `user_id` 或等价租户标识。查询、任务、缓存键、日志和权限检查都必须经过租户上下文。管理员查询也需要显式授权，不允许通过 ID 猜测访问其他用户数据。

### 4.3 凭证安全

- 优先 OAuth/Fast API，用户不需要手动复制 API 密钥。
- 任何 API Key 模式只开放读取和交易权限，不开放提币权限。
- 凭证加密后保存，业务代码只能获得短生命周期的受控使用权限。
- 凭证不得出现在前端、提示词、日志、错误信息、测试数据或代码仓库。
- 支持撤销、轮换和异常失效处理。

## 5. 核心数据模型

| 实体 | 关键内容 |
|---|---|
| User | 用户、地区、风险确认、状态 |
| ExchangeConnection | 用户、OKX 授权状态、权限、过期时间、凭证引用 |
| Strategy | 策略名称、版本、支持标的、状态、风险要求 |
| StrategyInstance | 用户、策略版本、配置、运行状态、模式 |
| RiskProfile | 单笔风险、最大仓位、杠杆、日亏损和交易次数上限 |
| Signal | 行情快照、策略版本、信号有效期、交易意图 |
| RiskDecision | 放行/拒绝、规则版本、限额快照、原因 |
| Order | 用户、客户端幂等键、交易所订单号、Builder Code、状态 |
| Fill/PositionSnapshot | 成交明细和持仓快照 |
| AuditEvent | 操作者、时间、动作、结果、关联对象 |
| SystemCircuitBreaker | 触发原因、范围、时间、恢复审批 |

## 6. 订单执行与一致性

订单执行必须采用以下流程：

1. 策略生成交易意图。
2. 生成唯一幂等键。
3. 风控服务保存并批准或拒绝。
4. 执行服务提交订单。
5. 记录交易所响应，不把请求成功当成成交。
6. 轮询或订阅订单状态。
7. 对账本地状态、交易所状态和成交记录。
8. 异常进入人工处理或自动熔断。

重试规则：网络超时不能直接重新下单，必须先按幂等键和交易所订单查询接口确认上一次请求结果。

## 7. 故障策略

以下情况默认“停止新增风险”：

- 行情过期或时间戳异常；
- 账户授权过期或状态未知；
- 订单状态无法确认；
- 风控服务不可用；
- 数据库无法写入审计记录；
- Builder Code 无法写入或归因校验失败；
- 发现重复订单或租户隔离异常；
- 触发用户或平台熔断。

恢复必须具备：故障原因、影响范围、未完成订单清单、人工确认和恢复记录。

## 8. 可观测性与安全审查

上线前必须定义并监控：

- 行情延迟和数据缺口；
- 策略运行成功率；
- 风控拒绝率和拒绝原因；
- 订单提交、成交、失败和对账延迟；
- 重复订单检测；
- OAuth 失效率；
- Builder 归因成功率；
- 熔断次数和恢复时长。

安全评审至少覆盖：威胁建模、权限矩阵、凭证泄露、重放/重复下单、越权读取、日志脱敏、备份恢复和管理员操作审计。

## 9. 架构评审闸门

编码前必须完成并通过评审：

1. 组件边界图；
2. 数据模型和租户隔离说明；
3. OAuth 生命周期设计；
4. 订单状态机和幂等方案；
5. 风控规则清单；
6. 故障、熔断和恢复流程；
7. 权限矩阵和密钥处理方案；
8. 监控与告警指标表。

