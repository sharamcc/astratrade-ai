# AstraTrade AI：OKX Builder / OAuth 接入与测试剧本

**版本**：0.1
**状态**：方案评审稿
**日期**：2026-09-20
**本阶段**：只准备接入和测试，不创建凭证、不执行真实交易

## 1. 接入目标

AstraTrade AI 面向多用户 Web/App 产品，规划采用 OKX OAuth 让用户授权自己的账户，再由后端在授权范围内调用 OKX OpenAPI。每笔符合条件的下单请求必须携带正确的 AI Builder Code，以便在 OKX 工作台中完成交易归因。

参考：[OKX AI Builder Program 接入指南](https://www.okx.com/zh-hans/help/ai-builder-program-integration-guide)

## 2. 申请材料

| 字段 | 准备内容 |
|---|---|
| 项目名称 | AstraTrade AI |
| 项目链接 | https://github.com/sharamcc/astratrade-ai |
| 产品类型 | 面向多用户的 AI 交易 Agent / Web 产品 |
| 项目简介 | 基于 OKX 实时行情、用户授权和预设风控规则，执行已审核的交易策略；支持模拟验证、受限自动执行和完整审计；不承诺收益。 |
| 接入方式 | OAuth 授权 + 后端 OpenAPI 执行 |
| 安全说明 | 非托管、最小权限、禁用提币、凭证加密隔离、用户可撤销授权、订单全流程审计 |

申请通过后需要保存：Builder Code、生效状态、适用协议、返佣规则版本、控制台截图或导出记录。返佣比例和金额不能在产品中作为收益承诺。

## 3. OAuth 配置清单

### 3.1 应用信息

- 应用名称：AstraTrade AI；
- Logo：使用仓库中的 `assets/astratrade-ai-logo.png`；
- 产品介绍：与申请材料保持一致；
- 生产和测试环境分开管理；
- Client ID 和 Client Secret 只允许后端使用。

### 3.2 URL 与网络配置

上线前准备并审核：

- 测试环境回调 URL；
- 生产环境回调 URL；
- 用户可见的隐私政策和服务条款 URL；
- OAuth 免鉴权 IP 配置；
- Fast API 相关 URL 和用户关联 IP 配置（如果最终接入方案需要）；
- 不使用个人电脑、临时隧道或不可控域名作为生产回调地址。

### 3.3 授权生命周期

必须定义：

1. 用户发起授权；
2. 回调校验成功；
3. 创建或关联用户连接记录；
4. 凭证安全保存；
5. 过期前刷新；
6. 过期、撤销或权限不足时暂停 Agent；
7. 用户主动撤销后清理可撤销凭证并保留审计记录；
8. 重新授权后恢复，但不能自动恢复此前被人工暂停的高风险 Agent。

## 4. Builder Code 归因规则

| 接入方式 | 必须使用的归因参数 |
|---|---|
| MCP | `aiBuilderCode` |
| CLI | `--aiBuilderCode <AI_BUILDER_CODE>` |
| 直接 OpenAPI | 请求中的 `tag` 字段 |
| OAuth 后端执行 | OAuth 取得授权后，最终 OpenAPI 下单仍使用正确的 `tag` 归因 |

禁止：把 MCP 参数写进 OpenAPI、把 CLI 参数改名、使用自定义字段替代官方归因字段，或在归因失败时继续扩大交易量。

## 5. 测试环境与数据原则

- 优先模拟盘；
- 使用专门测试账户，不使用个人主账户；
- 测试凭证不进入仓库、日志、截图或工单；
- 测试交易品种和订单规模提前固定；
- 每个测试用例记录用户、策略版本、订单意图、归因字段和最终结果；
- 任何实盘测试必须经过单独批准，并从最小规模开始。

## 6. 测试剧本

### A. 申请与配置

| ID | 场景 | 操作 | 预期结果 |
|---|---|---|---|
| A-01 | 申请材料 | 提交项目名称、链接、简介和接入方式 | 获得申请状态和 Builder Code 状态 |
| A-02 | 应用配置 | 配置应用名称、Logo、测试回调 URL | 配置可保存，前端不显示 Secret |
| A-03 | 错误回调 | 使用未登记的回调 URL | 授权失败，系统记录安全事件，不创建连接 |
| A-04 | 环境隔离 | 使用测试环境凭证访问生产环境 | 被拒绝或明确提示环境不匹配 |

### B. OAuth 生命周期

| ID | 场景 | 操作 | 预期结果 |
|---|---|---|---|
| B-01 | 首次授权 | 用户完成 OKX 授权 | 账户连接成功，显示权限和状态 |
| B-02 | 用户取消 | 用户在 OKX 页面取消授权 | AstraTrade 不创建有效连接 |
| B-03 | 授权过期 | 让授权进入过期状态 | Agent 自动暂停，不新增订单，用户收到通知 |
| B-04 | 主动撤销 | 用户点击撤销连接 | 新订单立即被拒绝，审计记录完整 |
| B-05 | 重新授权 | 用户重新完成授权 | 连接恢复，但被人工暂停的 Agent 不自动恢复 |
| B-06 | 多用户隔离 | 用户 A 查询用户 B 的连接 ID | 请求被拒绝且产生越权告警 |

### C. 策略与风控

| ID | 场景 | 操作 | 预期结果 |
|---|---|---|---|
| C-01 | 模拟信号 | 运行已发布策略 | 生成交易意图，不提交真实订单 |
| C-02 | 超限仓位 | 设置订单超过用户仓位上限 | 风控拒绝并说明规则 |
| C-03 | 超过日亏损 | 模拟达到日亏损阈值 | 触发用户熔断，后续订单全部拒绝 |
| C-04 | 过期行情 | 使用超出时效的行情快照 | 拒绝新增风险 |
| C-05 | 策略停用 | 停用当前策略版本 | 已运行实例暂停，不再生成可执行订单 |
| C-06 | 重复信号 | 重复提交同一信号 | 只保留一个幂等订单意图 |

### D. 订单与归因

| ID | 场景 | 操作 | 预期结果 |
|---|---|---|---|
| D-01 | 模拟下单 | 执行一笔符合条件的订单 | 订单状态完整流转并可对账 |
| D-02 | 归因字段 | 检查 OpenAPI 下单请求 | 使用正确 Builder Code 对应的 `tag` |
| D-03 | 请求超时 | 模拟提交后网络超时 | 先查询订单结果，不直接重复下单 |
| D-04 | 部分成交 | 模拟部分成交 | 本地状态与交易所状态保持一致 |
| D-05 | 订单拒绝 | 交易所拒绝订单 | 记录拒绝原因，不自动无限重试 |
| D-06 | 归因异常 | Builder Code 缺失或错误 | 阻止扩大交易量并触发告警 |
| D-07 | 控制台核对 | 在 OKX Builder 工作台查询测试交易 | 能识别来自 AstraTrade AI 的交易 |

### E. 小额实盘上线

| ID | 场景 | 操作 | 预期结果 |
|---|---|---|---|
| E-01 | 小额订单 | 使用批准的最小规模提交订单 | 订单成功提交，结果可查询 |
| E-02 | 真实成交 | 等待订单成交 | 成交与持仓结果完成对账 |
| E-03 | 返佣归因 | 检查 Builder 工作台 | 交易被正确识别；返佣以官方规则为准 |
| E-04 | 紧急暂停 | 交易过程中触发用户/平台暂停 | 不产生新的订单，未完成订单进入预定处理流程 |
| E-05 | 复盘 | 导出全链路记录 | 可从信号追溯到风控决定、订单、成交和归因 |

## 7. 失败处理与上线决策

以下任一情况出现时，停止扩大交易量：

- 控制台无法识别归因；
- Builder Code 状态或规则不明确；
- 订单成功提交但无法确认最终状态；
- 出现重复订单；
- 用户之间出现数据越权；
- 凭证进入日志、前端或仓库；
- OAuth 撤销后仍可下单；
- 风控拒绝规则未生效；
- 实际返佣与官方控制台无法核对。

只有在测试记录、错误处理、归因核对和安全评审全部通过后，才允许从测试账户扩大到受邀用户灰度。

## 8. 测试交付物

- Builder 申请记录；
- OAuth 配置清单；
- 测试账户和权限说明（不包含密钥）；
- 测试执行记录；
- 订单与归因核对表；
- 异常和熔断演练记录；
- 上线评审结论；
- 未解决问题与责任人清单。

